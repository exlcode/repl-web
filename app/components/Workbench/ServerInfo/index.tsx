import * as React from 'react'
import { FullSize } from 'styles/styledComponents'

interface IProps {}

export default class ServerInfo extends React.Component<IProps, {}> {
  render() {
    return <FullSize />
  }
}
